# 设计

## 目标

- 为 Codex、OpenCode 及其他兼容 `AGENTS.md`/Agent Skills 的编码 Agent 提供同一套可移植工作流。
- 将大部分任务时间和上下文用于问题解决、实现和有意义的证据。
- 让发现、规划、确认、验证和文档成本随风险增长。
- 保留项目意图、架构、用法、调查证据和可复用经验，同时避免每次修改都产生流程文书。
- 新内容和新方法在设计前借鉴权威资料与成熟实现，避免无依据地重复造轮子。
- 用复用已有证据的最小收尾关卡形成闭环，并为代码类修改生成可人工采用的 Commit 文稿。
- 每个 Agent 任务按日期和风险等级留下可追溯记录，同时让低等级任务保持一行成本。
- 复杂调查和重大修改按稳定编号追踪基线、重要调整与最终状态，使 TRACE 能解释修改如何演进。
- 需要持久行为契约的变更使用稳定规范 ID，把验收、测试、实现和验证连接起来，同时让普通缺陷保持无 Spec 成本。
- 用触达范围内的命名、文件职责和注释检查降低长期理解成本，不把普通修改升级成全仓治理。
- 让每日任务日志和修改轨迹按真实时间自上而下演进，既方便阅读也保留追加历史。
- 除必要的英文路径、名称和技术标识外，面向人的内容统一使用中文。

## 非目标

- 强制使用某个厂商的工具、模型、模式或权限系统。
- 每次变更都要求通读文档、运行完整测试、创建 TRACE 或更新概述文件。
- 替代项目自身的工程判断或 CI 规则。
- 充当项目管理器、问题追踪器或按时间排列的工作日志。
- 自动执行 `git commit`、`git push`，或为收尾机械重跑命令。
- 将每日任务日志变成第二套项目事实、完整调试记录或代码差异存档。
- 把 TRACE 写成工具调用流水、只含最终结果的重复总结或覆盖历史结论的可变文档。
- 对所有缺陷和重构强制完整 SDD、红灯或全仓 Spec 扫描。
- 建立无限增长、每轮必须通读的集中追溯矩阵。
- 要求每个函数都写注释、按固定行数拆文件，或在普通任务中扫描和重命名全仓代码。

## 必需行为

- `AGENTS.md` 必须先完成任务路由，再加载 Skill 或大范围上下文。
- L0/L1 默认不得加载开发 Skill。
- 局部缺陷必须使用精准修复流程，影响跨越既定边界时必须升级。
- 功能、重大和关键变更必须使用完整流程中的相应强度档位。
- 文档同步必须由实际影响触发。
- 安装和更新必须保留项目专属规则。
- 模板、脚本输出和 Skill 指令必须遵守中文优先约束。
- 新功能、新方法、新依赖、新协议或项目无成熟先例的方案必须先做有限网络调研；局部缺陷和既有模式修改不得为形式触发调研。
- 所有任务结束前执行最小收尾判断，证据充分时不得新增检查。
- 代码类修改必须提供基于实际差异的 Commit 文稿，不得虚构验证或自动提交。
- 每个任务必须记录开始与结束时间、等级和最终状态；L0/L1 一行，L2-L5 使用 `record-task-log` 的分级格式。
- 日志只能复用本轮已有证据，日志写入不得触发额外验证、知识同步或 Commit 文稿。
- 复杂 L2、方案会演进的 L3 按需建立 TRACE，L4/L5 在实质修改前必须建立；旧检查点通过新增更正或取代事件演进。
- L2 只有暴露持久契约缺口时才使用 Spec；L3 按行为影响选择轻量 Spec；L4/L5 涉及契约、数据、协议、迁移、安全或多客户端一致性时使用完整 Spec。
- Spec 使用稳定规范 ID 和就近追溯表；自动化红灯、回归测试、基线对比或人工验收按任务类型选择，不为形式制造失败。
- 代码类修改只检查本轮新增或实质修改的声明；局部歧义可在不扩大范围时修正，跨文件、公共契约或结构性问题按风险升级。
- 命名优先遵循项目领域词汇和语言官方约定；文件按职责所有权、依赖方向和变化原因拆分；注释解释契约、原因与不变量，不复述代码。
- worklog 与 TRACE 的日期文件按真实时间从旧到新排列，新任务写在下方；补录较早任务按真实时间归位，每项任务使用独立标题。

## 验收条件

- 克隆仓库后即可原生加载或直接阅读全部 Skill。
- 小修复无需通读所有概述文档，也无需运行聚合测试套件。
- 重大变更未核对受影响的行为和架构事实时不能结束。
- 包校验成功，安装预演和审计结果确定且可复现。
- 搜索面向人的英文句子时，仅剩确有必要保留的标准名称或技术标识。
- 标准任务的调研和收尾不会演变为固定的长篇搜索、全量测试或文档重读。
- 同一天的多项任务追加到同一文件，且未来 Agent 无需读取旧日志即可执行普通开发。
- TRACE 最终检查点能够按组件或文件说明“修改前 → 修改后”、原因、验证状态和未关闭风险。
- 启用 Spec 的任务能够从规范 ID 定位验收、测试或人工入口、实现符号、真实验证和状态；未启用时不产生空规格文件。
- 普通代码修改无需全仓扫描即可完成触达范围清晰度检查，复杂治理可定位到按需详细指南。
- 审计能够拒绝时间倒序的每日 worklog/TRACE，以及作为其他任务列表项存在的 L0/L1 记录。
