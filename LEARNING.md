# 项目经验

这里只保留开发本工作流过程中非显然、可复用的经验。

## `[workflow][context]` 必须先路由任务，再发现文档

- 触发：仓库中存在多个有价值的概述和历史文档。
- 规则：先判定任务等级，再读取可选文档或加载实现 Skill。
- 原因：一开始通读所有内容，会在找到真实问题范围前耗尽上下文预算。
- 证据：`docs/traces/2026-08-17-initial-workflow-design.md`。

## `[installer][permissions]` 修改根文件前先预检受管目录

- 触发：宿主允许写仓库根文件，但限制隐藏目录。
- 规则：改变根 `AGENTS.md` 前，先创建或确认 `.agents/` 可写。
- 原因：权限失败应保持目标项目不变，不能留下半安装状态。
- 证据：BookReader 首次接入暴露了这种分离权限情况。
