# 项目经验

这里只保留开发本工作流过程中非显然、可复用的经验。

## `[workflow][context]` 必须先路由任务，再发现文档

- 触发：仓库中存在多个有价值的概述和历史文档。
- 规则：先判定任务等级，再读取可选文档或加载实现 Skill。
- 原因：一开始通读所有内容，会在找到真实问题范围前耗尽上下文预算。
- 证据：`docs/traces/2026-08-17.md` 中 `T-legacy-01`。

## `[installer][permissions]` 修改根文件前先预检受管目录

- 触发：宿主允许写仓库根文件，但限制隐藏目录。
- 规则：改变根 `AGENTS.md` 前，先创建或确认 `.agents/` 可写。
- 原因：权限失败应保持目标项目不变，不能留下半安装状态。
- 证据：BookReader 首次接入暴露了这种分离权限情况。

## `[research][budget]` 新方案调研必须有停止条件

- 触发：开发项目中没有成熟先例的新功能、新方法、依赖或协议。
- 规则：优先比较 2～4 个权威来源；一致证据已覆盖重大风险后停止搜索，并回到项目约束做选择。
- 原因：完全跳过调研容易重复踩坑，无边界调研又会消耗实现时间和上下文。
- 证据：`docs/traces/2026-08-17.md` 中 `T-legacy-02`。

## `[closure][evidence]` 收尾复用证据，不启动第二轮验证

- 触发：实现和验证已经结束，准备交付结果。
- 规则：先用已有差异和验证回答最小收尾问题；只有明确缺口才补最小操作。
- 原因：把固定命令清单当作收尾，会重新引入本流程要消除的重复验证成本。
- 证据：`docs/traces/2026-08-17.md` 中 `T-legacy-02`。

## `[worklog][cost]` 每日日志必须按风险压缩

- 触发：要求每个 Agent 任务都留下时间和执行记录。
- 规则：L0/L1 只写一行，L2-L5 才加载分级日志 Skill；日志只追加已有结论，不复制完整输出。
- 原因：统一写成长日志会让简单任务的记录成本超过实际工作，也会制造第二套项目事实来源。
- 证据：`docs/traces/2026-08-17.md` 中 `T-164359`。

## `[trace][evolution]` 修改轨迹应追加意图变化而不是覆盖快照

- 触发：复杂调查或重大修改需要解释方案为什么变化以及最终落点。
- 规则：使用稳定任务编号追加基线、实质调整和最终映射；旧结论通过更正或取代事件演进。
- 原因：只写最终总结看不到修改路径，直接改写旧内容又会丢失曾经采用和放弃某个方向的原因。
- 证据：`docs/traces/2026-08-17.md` 中 `T-165442`。

## `[spec][cost]` 规格驱动必须按行为风险启用

- 触发：需要把功能行为长期连接到验收、测试、实现与验证。
- 规则：普通 L2 不创建 Spec；相关 L3 使用轻量档，涉及公共契约、数据、协议、迁移、安全或多客户端一致性的 L4/L5 使用完整档，并把追溯表放在对应功能 Spec 中。
- 原因：稳定规范 ID 能减少跨会话歧义，但固定通读 LEARNING、集中 TRACE 和全部 Spec 会让维护与 token 成本随历史线性增长。
- 证据：`docs/traces/2026-08-17.md` 中 `T-171842`；本地 `demo` SDD 对比，以及 GitHub Spec Kit、Kiro Specs、Agile Alliance ATDD 的有限调研。

## `[clarity][scope]` 清晰度治理必须限制在触达范围

- 触发：新增或修改的命名难以从使用处理解，单个文件混合职责，或注释缺失、失真。
- 规则：默认只检查本轮新增或实质修改的声明；命名使用领域词汇，文件按职责、依赖和变化原因划分，注释只补契约、原因和不变量。跨文件改名、公共契约迁移或结构性拆分按风险升级。
- 原因：全仓巡检、固定行数拆分和“所有函数加注释”会把局部工作变成无边界重构；完全忽略又会让理解成本持续积累。
- 证据：`docs/traces/2026-08-17.md` 中 `T-175010`；Kotlin Coding Conventions、Swift API Design Guidelines、Google Engineering Practices，以及本地 `demo` 的可维护性案例。

## `[history][order]` 每日记录应按真实时间从旧到新排列

- 触发：新的 worklog 或 TRACE 被写到文件顶部，或较早任务被补录在较晚任务之后。
- 规则：新任务通常追加在文件末尾；补录较早任务时移动完整记录块到真实时间位置；旧任务在后续继续时，在末尾追加同一任务编号的“继续”事件。所有任务使用独立标题。
- 原因：倒序记录不符合时间演进的阅读方式，L0/L1 列表项还会在 Markdown 结构上被误认为属于上一项大任务。
- 证据：`docs/traces/2026-08-17.md` 中 `T-175010`，以及审计对 BookReader 旧日志的失败基线。
