# Agent 修改轨迹 · 2026-08-17

- 时区：Asia/Shanghai（UTC+08:00）
- 说明：历史主题 TRACE 已迁移为当天追加式任务轨迹；时间不可恢复的条目使用 `legacy` 编号，不虚构执行时间。

## T-legacy-01 · L4 · 建立自适应开发工作流

### 时间未记录 · 开始基线

- 状态：历史迁移。
- 修改前：没有可移植的 `AGENTS.md` + Agent Skills 工作流，BookReader 也没有分级开发规则。
- 目标：建立与厂商无关、按风险分配上下文和验证成本的独立流程包，并完成首个项目接入。

### 时间未记录 · 重要调整

- 通用规则与项目规则从单一区块调整为独立受管区，原因是升级流程包时必须保留项目专属约束。
- 面向人的内容从英文调整为中文，仅保留路径、命令、代码标识符和标准名称。

### 时间未记录 · 最终映射

- 状态：完整完成。
- `无独立流程包` → 建立 `adaptive-agent-development`、安装/审计脚本和初始四个 Skill。
- `BookReader 无流程入口` → 安装根 `AGENTS.md`、项目 Skill、DESIGN、LEARNING 和接入证据。
- 验证：Skill、脚本、安装预演、首次安装、审计和强制刷新通过。
- 风险：不同 Agent 的原生 Skill 激活存在差异，使用直接读取 `SKILL.md` 作为回退。

## T-legacy-02 · L4 · 增加前置调研与最小收尾

### 时间未记录 · 开始基线

- 状态：历史迁移。
- 修改前：工作流覆盖任务前期分流和实施，但缺少新方案调研、低成本收尾和 Commit 文稿。
- 目标：在避免固定额外成本的前提下补齐方案质量和交付闭环。

### 时间未记录 · 调研与方案

- 参考 Codex、GitHub Copilot 和 OpenCode 的分层、就近、按需加载规则，保持根指令简洁。
- 参考 Conventional Commits，采用 `type(scope): 中文摘要`，不兼容变更使用 `!` 或 `BREAKING CHANGE:`。
- 来源：https://developers.openai.com/codex/guides/agents-md 、https://docs.github.com/en/copilot/customizing-copilot/adding-repository-custom-instructions-for-github-copilot 、https://opencode.ai/docs/rules/ 、https://www.conventionalcommits.org/en/v1.0.0/ 。

### 时间未记录 · 最终映射

- 状态：完整完成。
- `新方案依赖 Agent 记忆` → 新功能、新方法、新依赖和新协议先比较 2～4 个权威来源。
- `任务结束条件模糊` → 增加复用已有证据的最小收尾关卡。
- `用户自行整理提交说明` → 代码类修改后生成 Commit 文稿，但不自动提交或推送。
- 版本：0.1.0 → 0.2.0。

## T-164359 · L4 · 增加每日分级任务日志

### 16:43:59 · 开始基线

- 状态：进行中。
- 修改前：每项任务没有统一的日期、执行时间、等级和最终状态索引。
- 目标：一天一个 worklog，L0/L1 一行，L2-L5 按风险增加有效信息。

### 16:45:00 · 调研与方案

- 参考 Keep a Changelog 的人类可读、ISO 日期和避免倾倒 Git 日志原则。
- 参考 OpenTelemetry Span 的开始、结束、属性和状态模型；只借鉴结构，不引入遥测系统。
- 参考 Microsoft ADR，只让重大或难以回退的任务记录详细决策。
- 来源：https://keepachangelog.com/en/1.1.0/ 、https://opentelemetry.io/docs/concepts/signals/traces/ 、https://learn.microsoft.com/en-us/azure/well-architected/architect-role/architecture-decision-record 。

### 16:48:48 · 最终映射

- 状态：完整完成。
- `无任务执行索引` → 使用 `docs/worklogs/YYYY-MM-DD.md` 按结束顺序追加。
- `所有等级可能写同样长日志` → L0/L1 根规则一行，L2-L5 加载 `record-task-log`。
- `日志可能触发二次工作` → 明确日志不触发测试、构建、知识同步、TRACE 或 Commit 文稿。
- 安装/审计：必需 Skill 从四个增加为五个；缺失日志 Skill 时审计正确失败。
- 版本：0.2.0 → 0.3.0。
- worklog：`docs/worklogs/2026-08-17.md` 中同编号任务。

## T-165442 · L4 · 将 TRACE 改为追加式修改跟踪

### 16:54:42 · 开始基线

- 状态：进行中。
- 修改前：TRACE 按主题各建一个文件，主要在任务结束后记录结论，不能看到修改基线、方向变化和最终落点之间的关系。
- 目标：像 worklog 一样按日期聚合，但使用稳定任务编号和检查点跟踪修改演进；worklog 保持执行索引职责。
- 预计范围：根规则、新 TRACE Skill、开发/知识/日志 Skill、文档职责、安装审计、历史 TRACE 迁移、BookReader 接入与版本清单。

### 16:56:00 · 调研与方案

- `最终快照` → `追加式事件`：参考 Event Sourcing 保留表达修改意图的有序事件，并用新事件更正旧结论。
- `无关联的过程段落` → `稳定任务编号 + 检查点`：参考 OpenTelemetry 用同一工作单元关联开始、重要事件、结束和状态。
- `所有变化同等详细` → `只追踪重大决策`：参考 ADR 只保留影响结构、质量属性或难回退选择的上下文、取舍和后果。
- 来源：https://learn.microsoft.com/en-us/azure/architecture/patterns/event-sourcing 、https://opentelemetry.io/docs/concepts/signals/traces/ 、https://learn.microsoft.com/en-us/azure/well-architected/architect-role/architecture-decision-record 。

### 16:57:00 · 方案确定

- 新增 `track-change-trace`，复杂 L2、方案会演进的 L3 按需加载，L4/L5 强制加载。
- TRACE 改为每天一个 `docs/traces/YYYY-MM-DD.md`；同日任务用 `T-HHmmss` 区分。
- 检查点只在基线、实质调整和最终映射时追加，不记录每次工具调用。
- 旧 TRACE 不直接丢弃内容，而是迁移为 `legacy` 任务轨迹；无法恢复的时间明确标记“时间未记录”。

### 17:01:16 · 最终映射

- 状态：完整完成。
- `按主题分散且只记录最终结论` → 每天一个 TRACE 文件，以稳定编号追加开始、重要调整和最终状态；原因是需要保留修改演进。
- `TRACE 无专用执行规则` → 新增 `skills/track-change-trace/`；复杂 L2、方案会演进的 L3 按需使用，L4/L5 强制使用。
- `旧结论可被覆盖` → 更正和取代必须追加新检查点，旧检查点保持不变。
- `worklog 与 TRACE 无稳定关联` → 两者共享 `T-HHmmss`；worklog 索引执行结果，TRACE 跟踪修改演进。
- `三个主题 TRACE` → 迁移为 `docs/traces/2026-08-17.md` 中的历史任务与已知时间任务；无法恢复的时间不虚构。
- 安装/审计：必需 Skill 从五个增加为六个；缺失 `track-change-trace` 时审计正确失败。
- 验证：流程包与 BookReader 共 12 份 Skill、脚本编译、全新安装、0.4.0 审计、缺失 Skill 检测、项目专属区保留和差异检查通过。
- 文档：README、DESIGN、ARCHITECTURE、LEARNING、模板和文档职责已同步；版本 0.3.0 → 0.4.0。
- 剩余风险：并发 Agent 追加同一天文件仍可能竞争，保持为出现实际需求后再处理。
- worklog：`docs/worklogs/2026-08-17.md` 中 `T-165442`。
